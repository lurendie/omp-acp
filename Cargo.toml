[package]
name = "omp-acp"
version = "0.1.0"
edition = "2021"
description = "Oh My Pi (omp) agent for Zed"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"

[profile.release]
opt-level = "s"
lto = true
